#include <bits/stdc++.h>
using namespace std;
#define all(x) (x).begin(), (x).end()

int main() {
    int n; cin >> n;
    vector<int> altered(n);

    for (int& a : altered) cin >> a;

    auto original = altered;
    sort(all(original));

    // find bessie
    for (int i = 0; i < n - 1; i++) {
        if (i == 0 && i )
    }
}