#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <cmath>
#include <map>
#include <set>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // input
    // example: int n; cin >> n;

    // solution logic here

    // output
    // example: cout << n << "\n";

    return 0;
}
